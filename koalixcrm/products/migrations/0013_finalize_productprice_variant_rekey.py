# -*- coding: utf-8 -*-
"""Phase 3 of the `ProductPrice.product_type` -> `ProductPrice.variant` FK
lift (ADR-0021 Amendment 2026-06-28 / ADR-0005 Amendment): drops the legacy
`product_type` FK and makes `variant` non-nullable, now that migration 0012
has backfilled every row. Hand-written (not autogenerated) because the
non-null alteration needs no default — 0012 already guarantees every row
has a `variant_id`."""
from __future__ import annotations

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("products", "0012_backfill_productprice_variant"),
    ]

    operations = [
        migrations.RemoveField(
            model_name="productprice",
            name="product_type",
        ),
        migrations.AlterField(
            model_name="productprice",
            name="variant",
            field=models.ForeignKey(
                on_delete=django.db.models.deletion.CASCADE,
                related_name="prices",
                to="products.productvariant",
                verbose_name="Product Variant",
            ),
        ),
    ]
